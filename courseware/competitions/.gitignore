# Generated by each package's prepare_data.py — deterministic, so rebuild
# rather than commit. X_test.csv alone is 9 MB.
*/data/
__pycache__/
*.pyc

# The dataset id salt — see _dataset_ids.py. Never commit.
.id-salt

# Teacher-only expert pipelines: the answer to a challenge. The repository is
# public, so they stay local. See s2-dpe-energy-label/teacher_expert_pipeline.py.
*/teacher_*
